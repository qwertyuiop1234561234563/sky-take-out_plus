<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal (name, category_id, price, description, status, create_time, update_time, create_user, update_user)
        values
           #{setmeal.name},
            #{setmeal.categoryId},
            #{setmeal.price},
            #{setmeal.description},
            #{setmeal.status},
            #{setmeal.createTime},
            #{setmeal.updateTime},
            #{setmeal.createUser},
            #{setmeal.updateUser}
    </insert>
    <update id="update">
        update setmeal
        set
            category_id = #{setmeal.categoryId},
            price = #{setmeal.price},
            name = #{setmeal.name},
            image = #{setmeal.image},

            <if test="description != null">
                description = #{setmeal.description},
            </if>
            <if test="status != null">
                status = #{setmeal.status},
            </if>
            update_time = #{setmeal.updateTime},
            update_user = #{setmeal.updateUser}
        where
            id = #{setmeal.id}
    </update>
    <select id="selectPage" resultType="com.sky.vo.SetmealVO">
        select
        s.*,c.name categoryName
        from
        setmeal s
        left join
        category c
        on
        s.category_id = c.id
        <where>
            <if test="name != null">
                and s.name like concat('%',#{name},'%')
            </if>
            <if test="status != null">
                and s.status = #{status}
            </if>
            <if test="categoryId != null">
                and s.category_id = #{categoryId}
            </if>
        </where>
        order by s.create_time desc
    </select>


</mapper>
